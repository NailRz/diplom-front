import { createContext } from "react";

export const AuthContext = createContext(null);
export const IsTestCompleteContext = createContext(null);
export const ResultWpm = createContext(null);
export const ResultTime = createContext(null);
